# Configuration file for database connection
# Copy this to .env and fill in your actual values

DB_HOST=localhost
DB_NAME=siliconflow_tracker
DB_USER=root
DB_PASS=
DB_PORT=3306

# Security - Generate a random 32-character encryption key
# Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# Linux/Mac: openssl rand -base64 32
ENCRYPTION_KEY=CHANGE_THIS_TO_A_RANDOM_32_CHARACTER_KEY

# API Configuration
SILICONFLOW_API_URL=https://api.siliconflow.cn/v1/user/info

# CORS Settings (use * for development, specify domain for production)
CORS_ORIGIN=*
